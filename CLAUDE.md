# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — no source files, no commits, no README, no build configuration. There is no codebase to document yet.

When code is added, this file should be updated to cover:

- **Build / run / test commands** — including how to run a single test once a test runner is chosen.
- **High-level architecture** — the cross-file structure that isn't obvious from reading any single file.
- **Project-specific conventions** — anything a future Claude session would otherwise have to rediscover.

## Working in this repo

- Active development branch for documentation changes: `claude/add-claude-documentation-Vi3zH`.
- Remote: `origin` points at `baghb004-cpu/lusik-and-sons-` (GitHub).
- There are no Cursor rules, Copilot instructions, or README to inherit guidance from at this time.
