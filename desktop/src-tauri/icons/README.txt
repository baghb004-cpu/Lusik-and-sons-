Generate the icon set before building:
  npx @tauri-apps/cli icon path/to/your-1024.png
(That writes icon.ico + the platform set here. Any square PNG works as a
placeholder — the Lusik gold-on-ink mark is the obvious choice.)
