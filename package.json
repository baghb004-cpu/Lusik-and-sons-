{
  "name": "lusik-and-sons",
  "private": true,
  "version": "0.0.0",
  "description": "Root package.json so Netlify installs function dependencies (esp. `stripe`, which esbuild can't statically bundle and so must be present in node_modules at runtime).",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0",
    "@netlify/functions": "^2.8.0",
    "@netlify/neon": "^0.1.0",
    "stripe": "^17.0.0"
  }
}
