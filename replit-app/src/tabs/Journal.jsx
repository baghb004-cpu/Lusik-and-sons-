// Journal — Chunk 7 fills this with the editorial card list + post pages
// (the web sibling of ios/LusikSons/Views/JournalView.swift).

import React from "react";

export function Journal() {
  return (
    <div className="placeholder-panel">
      <h2>Journal — Chunk 7</h2>
      <p>Lusik's posts — the alphabet, the cross-stitch, the pomegranate.</p>
    </div>
  );
}
