// Shop — Chunk 1 fills this with the category index → product grids →
// detail pages (the web sibling of ios/LusikSons/Views/ShopView.swift).

import React from "react";

export function Shop() {
  return (
    <div className="placeholder-panel">
      <h2>Products — Chunk 1</h2>
      <p>Category cards, product grids, and the classic detail page land here.</p>
    </div>
  );
}
